# random-forest
This project uses the Random Forest algorithm to predict Titanic passenger survival. The dataset was cleaned by handling missing values and encoding categorical features. The model was trained and evaluated using accuracy and a confusion matrix, and feature importance was analyzed to identify key factors influencing survival predictions.
